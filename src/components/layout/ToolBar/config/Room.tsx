const ToolBarDetails = ({ info }: { info: any }) => {
    return (
        <div className="flex flex-col gap-2 w-full">
            <div className="flex gap-2 items-center">
                <div className="flex gap-2 items-center">
                    Olá Character
                </div>
            </div>
        </div>
    );
};

export default ToolBarDetails;